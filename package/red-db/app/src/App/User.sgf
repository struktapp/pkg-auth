<?php

namespace {{app}};

class User extends \App\Contract\RedDbModel{

	/**
	* @Type(string)
	*/
	public  $username;

	/**
	* @Type(string)
	*/
	public  $password;

	/**
	* @Type(enum)
	* @List(ACTIVE,INACTIVE,PENDING)
	*/
	public  $status;

	/**
	* @Type(int)
	*/
	public  $role_id;

}