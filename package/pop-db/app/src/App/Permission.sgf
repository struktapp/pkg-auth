<?php

namespace {{app}};

class Permission extends \App\Contract\PopDbModel{

	/**
	* @Type(string)
	*/
	public  $name;

	/**
	* @Type(text)
	*/
	public  $descr;

	/**
	* @Type(string)
	*/
	public  $tags;

	/**
	* @Type(datetime)
	* @Default(now)
	*/
	public  $created_at;
}